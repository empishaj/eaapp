import {$,esc,md,toast,formatRelative,pct} from '../core/utils.js';
import {isDue,reviewCard,cardScore} from '../core/srs.js';

const NEW_DAILY=10;
let examTimer=null;
const avg=a=>a.length?Math.round(a.reduce((x,y)=>x+y,0)/a.length):0;
const stateOf=(state,id)=>state.bsiCardState?.[id];
const scoreOf=(state,id)=>cardScore(stateOf(state,id));
const moduleCards=(content,id)=>content.bsiCardsByModule[id]||[];
const topicCards=(content,id)=>content.bsiCardsByTopic[id]||[];
const introduced=(state,cards)=>cards.filter(c=>stateOf(state,c.id)).length;
const mastered=(state,cards)=>cards.filter(c=>scoreOf(state,c.id)>=70).length;
const mastery=(state,cards)=>avg(cards.map(c=>scoreOf(state,c.id)));
const dueReviewed=(state,cards)=>cards.filter(c=>stateOf(state,c.id)&&isDue(stateOf(state,c.id)));
const unseen=(state,cards)=>cards.filter(c=>!stateOf(state,c.id));
const moduleName=(content,id)=>content.bsiModules.find(m=>m.id===id)?.module||id;
const safeHash=s=>encodeURIComponent(s);
function stopExamTimer(){if(examTimer){clearInterval(examTimer);examTimer=null}}
function getStudyQueue(state,cards){const due=dueReviewed(state,cards);const fresh=unseen(state,cards).slice(0,NEW_DAILY);return [...due,...fresh]}
function typeBadge(c){return `${c.card_type} · L${c.difficulty}`}
function progressBar(v){return `<div class="progress"><span style="width:${Math.max(0,Math.min(100,v))}%"></span></div>`}

export function renderBSIAcademy(ctx){stopExamTimer();const {state,content}=ctx,app=$('#app');const total=content.bsiCards.length,intro=introduced(state,content.bsiCards),mast=mastery(state,content.bsiCards),due=dueReviewed(state,content.bsiCards).length;
  app.innerHTML=`<section class="page-head"><div><a class="small muted" href="#academy">← Akademie</a><div class="eyebrow">BSI Master Academy</div><h1>IT-Grundschutz im Behördenkontext</h1><p class="lead">${total} ausführliche Lernkarten führen von Informationssicherheits-Grundlagen über ISMS und Grundschutzmethodik bis zum Transfer in Enterprise Architecture und Behördenpraxis.</p></div><div class="mastery-ring" style="--p:${mast};width:112px"><div><strong>${mast}%</strong><small>Mastery</small></div></div></section>
  <section class="grid-4"><div class="card metric"><strong>${total}</strong><small>Lernkarten</small></div><div class="card metric"><strong>${intro}</strong><small>eingeführt</small></div><div class="card metric"><strong>${due}</strong><small>Wiederholungen fällig</small></div><div class="card metric"><strong>${Object.keys(content.bsiTopicMeta).length}</strong><small>Kernthemen</small></div></section>
  <section class="hero section"><div class="hero-grid"><div><div class="eyebrow">Prüfung + Handlungsfähigkeit</div><h2>Vom Begriff zur sicheren Anwendung</h2><p class="lead">Lerne zuerst systematisch nach Themen. Wiederhole anschließend aktiv. Der Prüfungssimulator trennt Training und Prüfung konsequent.</p><div class="hero-actions"><a class="btn btn-primary" href="#bsi-review">Jetzt lernen</a><a class="btn btn-soft" href="#bsi-exam">Prüfungssimulator</a></div></div><div class="bsi-mark">BSI</div></div></section>
  <section class="section"><div class="section-head"><div><div class="eyebrow">Curriculum</div><h2>${content.bsiModules.length} Lernmodule</h2><p class="muted">Jedes Modul bündelt zusammengehörige Themen und baut auf den vorherigen Grundlagen auf.</p></div></div><div class="bsi-module-grid">${content.bsiModules.map((m,i)=>{const cards=moduleCards(content,m.id),mi=mastery(state,cards),inn=introduced(state,cards);return `<a class="card interactive bsi-module" href="#bsimodule=${m.id}"><div class="bsi-module-top"><span class="course-index">${m.id}</span><span class="badge">${cards.length} Karten</span></div><h3>${esc(m.module.replace(/^\d+\s*/,''))}</h3><p class="small muted">${m.topics.length} Themen · ${inn}/${cards.length} eingeführt</p>${progressBar(mi)}<div class="artifact-meta"><span>${mi}% Mastery</span><span>${mastered(state,cards)} sicher</span></div></a>`}).join('')}</div></section>
  <section class="card section"><h3>Quellenprinzip</h3><p>Die Karten sind eigenständige didaktische Zusammenfassungen. Aktuelle offizielle BSI-Quellen haben Vorrang vor Schulungsflyern oder älteren Unterlagen. Prüfungsfragen sind neu formulierte Trainingsfragen und keine Originalprüfungsfragen.</p></section>`;
}

export function renderBSIModule(ctx,id){stopExamTimer();const {state,content}=ctx,app=$('#app'),m=content.bsiModules.find(x=>x.id===id);if(!m)return app.innerHTML='<div class="empty">BSI-Modul nicht gefunden.</div>';const cards=moduleCards(content,id),mi=mastery(state,cards);
  const topics=Object.values(content.bsiTopicMeta).filter(t=>t.moduleId===id).sort((a,b)=>Number(a.id.replace(/\D/g,''))-Number(b.id.replace(/\D/g,'')));
  app.innerHTML=`<section class="page-head"><div><a class="small muted" href="#bsi">← BSI Master Academy</a><div class="eyebrow">Modul ${esc(id)}</div><h1>${esc(m.module.replace(/^\d+\s*/,''))}</h1><p class="lead">${cards.length} Karten · ${topics.length} Kernthemen · systematisch vom Grundlagenwissen zum Behörden- und EA-Transfer.</p></div><div class="mastery-ring" style="--p:${mi};width:108px"><div><strong>${mi}%</strong><small>Mastery</small></div></div></section>
  <section class="section"><div class="lesson-list">${topics.map((t,i)=>{const tc=topicCards(content,t.id),tm=mastery(state,tc),intro=introduced(state,tc);return `<a class="lesson-item" href="#bsitopic=${t.id}"><span class="num">${String(i+1).padStart(2,'0')}</span><span><strong>${esc(t.title)}</strong><small class="muted" style="display:block">${tc.length} Karten · ${intro} eingeführt · ${mastered(state,tc)} sicher</small><span class="lesson-meta">${[...new Set(tc.map(c=>c.card_type))].slice(0,4).map(x=>`<span class="badge">${esc(x)}</span>`).join('')}</span></span><span class="score">${tm}%</span></a>`}).join('')}</div></section>`;
}

export function renderBSITopic(ctx,id){stopExamTimer();const {state,content}=ctx,app=$('#app'),t=content.bsiTopicMeta[id],cards=topicCards(content,id);if(!t||!cards.length)return app.innerHTML='<div class="empty">BSI-Thema nicht gefunden.</div>';const tm=mastery(state,cards),due=dueReviewed(state,cards).length;
  app.innerHTML=`<section class="page-head"><div><a class="small muted" href="#bsimodule=${t.moduleId}">← ${esc(t.module.replace(/^\d+\s*/,''))}</a><div class="eyebrow">${esc(id)} · BSI-Thema</div><h1>${esc(t.title)}</h1><p class="lead">${cards.length} ausführliche Karten mit Grundlagen, Abgrenzung, Vorgehen, Behördenfall, Audit/Nachweis, EA-Transfer und Prüfungstraining.</p></div><div class="mastery-ring" style="--p:${tm};width:108px"><div><strong>${tm}%</strong><small>Mastery</small></div></div></section>
  <section class="grid-3"><div class="card metric"><strong>${introduced(state,cards)}/${cards.length}</strong><small>eingeführt</small></div><div class="card metric"><strong>${mastered(state,cards)}</strong><small>sicher</small></div><div class="card metric"><strong>${due}</strong><small>fällig</small></div></section>
  <section class="hero section"><div><h2>Thema aktiv lernen</h2><p class="lead">Die Antworttexte sind bewusst ausführlich. Erst selbst antworten, dann vergleichen und ehrlich bewerten.</p><div class="hero-actions"><a class="btn btn-primary" href="#bsi-study=${id}">Karten lernen</a><a class="btn btn-soft" href="#bsi-review-topic=${id}">Nur fällige wiederholen</a></div></div></section>
  <section class="section"><div class="section-head"><div><h2>Kartenkatalog</h2><p class="muted">Du kannst jede Karte auch gezielt öffnen.</p></div></div><div class="lesson-list">${cards.map((c,i)=>`<a class="lesson-item" href="#bsicard=${c.id}"><span class="num">${String(i+1).padStart(2,'0')}</span><span><strong>${esc(c.card_type)}</strong><small class="muted" style="display:block">${esc(c.front.split('\n')[0])}</small><span class="lesson-meta"><span class="badge">Level ${c.difficulty}</span><span class="badge">${c.estimated_minutes} min</span></span></span><span class="score">${scoreOf(state,c.id)}%</span></a>`).join('')}</div></section>`;
}

function renderCardShell(ctx,c,backHref,nextHref=null,indexText=''){const {state}=ctx,app=$('#app'),s=stateOf(state,c.id);app.innerHTML=`<section class="page-head"><div><a class="small muted" href="${backHref}">← Zurück</a><div class="eyebrow">${esc(c.module)}${indexText?` · ${esc(indexText)}`:''}</div><h1>${esc(c.topic)}</h1></div><span class="badge">${esc(typeBadge(c))}</span></section><section class="card bsi-learning-card"><div class="bsi-card-meta"><span class="badge">${esc(c.track)}</span><span class="small muted">${s?formatRelative(s.dueAt):'Neu'} · ca. ${c.estimated_minutes} min</span></div><div class="bsi-question">${md(c.front)}</div><div id="bsiAnswer" class="bsi-answer hidden">${md(c.back)}<div class="bsi-sources"><strong>Quellenbasis</strong><ul>${c.sources.map(x=>`<li>${esc(x)}</li>`).join('')}</ul><p class="tiny muted">${esc(c.source_note||'')}</p></div></div><button id="bsiShow" class="btn btn-primary">Antwort anzeigen</button><div id="bsiRatings" class="rating-row hidden">${['Nochmal','Schwer','Gut','Leicht'].map((x,r)=>`<button data-bsi-rate="${r}">${x}</button>`).join('')}</div></section><p class="small muted">Bewerte, ob du die Kernaussage ohne Hilfe erklären und auf einen Behördenfall übertragen konntest.</p>`;
  $('#bsiShow').onclick=()=>{$('#bsiAnswer').classList.remove('hidden');$('#bsiRatings').classList.remove('hidden');$('#bsiShow').classList.add('hidden')};
  return nextHref;
}

export function renderBSICard(ctx,id){stopExamTimer();const c=ctx.content.bsiCardById[id];if(!c){$('#app').innerHTML='<div class="empty">BSI-Karte nicht gefunden.</div>';return}renderCardShell(ctx,c,`#bsitopic=${c.topic_id}`);document.querySelectorAll('[data-bsi-rate]').forEach(b=>b.onclick=()=>{const r=Number(b.dataset.bsiRate);ctx.state.bsiCardState[c.id]=reviewCard(ctx.state.bsiCardState[c.id],r);ctx.log('bsi-card-review',{cardId:c.id,topicId:c.topic_id,rating:r});ctx.save();toast('Lernstand gespeichert');renderBSICard(ctx,id)})}

function renderStudy(ctx,cards,backHref,title){stopExamTimer();const {state}=ctx,app=$('#app');if(!cards.length){app.innerHTML=`<section class="page-head"><div><a class="small muted" href="${backHref}">← Zurück</a><h1>${esc(title)}</h1></div></section><div class="card empty">Keine Karten in dieser Auswahl.</div>`;return}let i=0;const show=()=>{const c=cards[i];renderCardShell(ctx,c,backHref,null,`${i+1}/${cards.length}`);document.querySelectorAll('[data-bsi-rate]').forEach(b=>b.onclick=()=>{const r=Number(b.dataset.bsiRate);state.bsiCardState[c.id]=reviewCard(state.bsiCardState[c.id],r);ctx.log('bsi-card-review',{cardId:c.id,topicId:c.topic_id,rating:r});ctx.save();i++;if(i>=cards.length){toast('BSI-Session abgeschlossen');location.hash=backHref.slice(1)}else show()})};show()}
export function renderBSIStudy(ctx,topicId){const cards=topicCards(ctx.content,topicId);renderStudy(ctx,cards,`#bsitopic=${topicId}`,ctx.content.bsiTopicMeta[topicId]?.title||'BSI lernen')}
export function renderBSIReviewTopic(ctx,topicId){let cards=dueReviewed(ctx.state,topicCards(ctx.content,topicId));if(!cards.length)cards=unseen(ctx.state,topicCards(ctx.content,topicId)).slice(0,NEW_DAILY);renderStudy(ctx,cards,`#bsitopic=${topicId}`,'Fällige BSI-Karten')}
export function renderBSIReview(ctx){const cards=getStudyQueue(ctx.state,ctx.content.bsiCards);renderStudy(ctx,cards,'#bsi','BSI Review')}

function parseOptions(card){const lines=card.front.split('\n');const question=lines.filter(x=>!/^\s*[A-D]\.\s/.test(x)).join(' ').trim();const options=lines.filter(x=>/^\s*[A-D]\.\s/.test(x)).map(x=>({key:x.trim()[0],text:x.trim().slice(3)}));return {question,options}}
function expected(card){const m=card.back.match(/\*\*Lösung:\*\*\s*([A-D](?:,\s*[A-D])*(?:\s+und\s+[A-D])?)/);return m?[...m[1].matchAll(/[A-D]/g)].map(x=>x[0]):[]}
function sameSet(a,b){return a.length===b.length&&[...a].sort().every((x,i)=>x===[...b].sort()[i])}
function shuffle(a){a=[...a];for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]]}return a}
function newExam(content){const qs=shuffle(content.bsiCards.filter(c=>c.card_type==='Prüfungsfrage')).slice(0,50);const permutations={};for(const c of qs)permutations[c.id]=shuffle(['A','B','C','D']);return {startedAt:new Date().toISOString(),durationSec:3600,questionIds:qs.map(c=>c.id),permutations,answers:{},current:0,completed:false}}
function secondsLeft(s){return Math.max(0,s.durationSec-Math.floor((Date.now()-new Date(s.startedAt).getTime())/1000))}
function timeFmt(sec){return `${String(Math.floor(sec/60)).padStart(2,'0')}:${String(sec%60).padStart(2,'0')}`}
function displayExam(card,perm){const p=parseOptions(card),by=Object.fromEntries(p.options.map(o=>[o.key,o.text])),order=perm||['A','B','C','D'];const options=order.map((orig,i)=>({key:['A','B','C','D'][i],orig,text:by[orig]}));const expOrig=expected(card),exp=options.filter(o=>expOrig.includes(o.orig)).map(o=>o.key);return {question:p.question,options,exp}}
function examResult(session,content){let correct=0;const details=session.questionIds.map(id=>{const c=content.bsiCardById[id],got=session.answers[id]||[],disp=displayExam(c,session.permutations?.[id]),exp=disp.exp,ok=sameSet(got,exp);if(ok)correct++;return {c,got,exp,ok}});return {correct,total:details.length,percent:Math.round(correct/details.length*100),details}}
function finishExam(ctx,auto=false){stopExamTimer();const s=ctx.state.bsiExamSession;if(!s)return;s.completed=true;s.completedAt=new Date().toISOString();const r=examResult(s,ctx.content);ctx.state.bsiExamHistory.unshift({at:s.completedAt,correct:r.correct,total:r.total,percent:r.percent,auto});ctx.state.bsiExamHistory=ctx.state.bsiExamHistory.slice(0,20);ctx.save();renderBSIExam(ctx,true)}
export function renderBSIExam(ctx,showResult=false){stopExamTimer();const {state,content}=ctx,app=$('#app');let s=state.bsiExamSession;
  if(showResult||s?.completed){if(!s){location.hash='bsi-exam';return}const r=examResult(s,content),wrong=r.details.filter(x=>!x.ok);app.innerHTML=`<section class="page-head"><div><a class="small muted" href="#bsi">← BSI Master Academy</a><div class="eyebrow">Prüfungsauswertung</div><h1>${r.correct}/${r.total} richtig</h1><p class="lead">${r.percent}% · bewertet wird jeweils die vollständige Kombination der ausgewählten Aussagen.</p></div><div class="mastery-ring" style="--p:${r.percent};width:112px"><div><strong>${r.percent}%</strong><small>Score</small></div></div></section><section class="grid-3"><div class="card metric"><strong>${r.correct}</strong><small>korrekt</small></div><div class="card metric"><strong>${wrong.length}</strong><small>falsch</small></div><div class="card metric"><strong>${state.bsiExamHistory.length}</strong><small>Simulationen</small></div></section><section class="section"><div class="section-head"><div><h2>Fehleranalyse</h2><p class="muted">Öffne die zugehörigen Lernkarten und arbeite die Begründung nach.</p></div></div><div class="lesson-list">${wrong.map((x,i)=>`<a class="lesson-item" href="#bsicard=${x.c.id}"><span class="num">${String(i+1).padStart(2,'0')}</span><span><strong>${esc(x.c.topic)}</strong><small class="muted" style="display:block">Deine Auswahl: ${esc(x.got.join(', ')||'–')} · erwartet: ${esc(x.exp.join(', '))}</small></span><span class="score">Review</span></a>`).join('')||'<div class="card empty">Alle Fragen korrekt.</div>'}</div></section><div class="hero-actions"><button id="newExam" class="btn btn-primary">Neue Prüfung starten</button><a class="btn btn-soft" href="#bsi">Zur Akademie</a></div>`;$('#newExam').onclick=()=>{state.bsiExamSession=newExam(content);ctx.save();renderBSIExam(ctx)};return}
  if(!s){const history=state.bsiExamHistory||[];app.innerHTML=`<section class="page-head"><div><a class="small muted" href="#bsi">← BSI Master Academy</a><div class="eyebrow">Prüfungssimulator</div><h1>50 Fragen · 60 Minuten</h1><p class="lead">Prüfungsnahes Training mit vier Aussagen je Frage. Eine, mehrere, alle oder keine können richtig sein. Während der Simulation werden keine Lösungen angezeigt.</p></div></section><section class="hero"><h2>Simulation starten</h2><p>Die 50 Fragen werden zufällig aus ${content.bsiCards.filter(c=>c.card_type==='Prüfungsfrage').length} eigens formulierten Prüfungskarten gewählt. Antworten werden lokal gespeichert.</p><button id="examStart" class="btn btn-primary">Prüfung starten</button></section>${history.length?`<section class="section"><h2>Letzte Ergebnisse</h2><div class="lesson-list">${history.slice(0,8).map((h,i)=>`<div class="lesson-item"><span class="num">${i+1}</span><span><strong>${h.percent}%</strong><small class="muted" style="display:block">${new Date(h.at).toLocaleString('de-DE')} · ${h.correct}/${h.total}</small></span><span class="score">${h.percent}%</span></div>`).join('')}</div></section>`:''}`;$('#examStart').onclick=()=>{state.bsiExamSession=newExam(content);ctx.save();renderBSIExam(ctx)};return}
  if(secondsLeft(s)<=0){finishExam(ctx,true);return}
  const i=Math.min(s.current||0,s.questionIds.length-1),c=content.bsiCardById[s.questionIds[i]],p=displayExam(c,s.permutations?.[c.id]),ans=s.answers[c.id]||[];
  app.innerHTML=`<section class="page-head"><div><a class="small muted" href="#bsi">← Prüfung verlassen</a><div class="eyebrow">BSI Prüfung · Frage ${i+1}/${s.questionIds.length}</div><h1>${esc(c.topic)}</h1></div><div class="exam-clock" id="examClock">${timeFmt(secondsLeft(s))}</div></section><section class="card exam-card"><div class="practice-question">${esc(p.question)}</div><div class="exam-options">${p.options.map(o=>`<label class="exam-option"><input type="checkbox" value="${o.key}" ${ans.includes(o.key)?'checked':''}><span><strong>${o.key}.</strong> ${esc(o.text)}</span></label>`).join('')}</div><div class="exam-nav"><button id="examPrev" class="btn btn-soft" ${i===0?'disabled':''}>← Zurück</button><span class="small muted">${Object.keys(s.answers).length}/${s.questionIds.length} beantwortet</span><button id="examNext" class="btn btn-primary">${i===s.questionIds.length-1?'Antwort speichern':'Speichern & weiter'} →</button></div></section><section class="section"><div class="exam-jump">${s.questionIds.map((id,j)=>`<button data-exam-jump="${j}" class="${j===i?'active':''} ${s.answers[id]?.length?'answered':''}">${j+1}</button>`).join('')}</div></section><div class="hero-actions"><button id="examFinish" class="btn btn-danger">Prüfung abgeben</button></div>`;
  const saveAnswer=()=>{s.answers[c.id]=[...document.querySelectorAll('.exam-option input:checked')].map(x=>x.value);ctx.save()};
  $('#examPrev').onclick=()=>{saveAnswer();s.current=Math.max(0,i-1);ctx.save();renderBSIExam(ctx)};
  $('#examNext').onclick=()=>{saveAnswer();s.current=Math.min(s.questionIds.length-1,i+1);ctx.save();renderBSIExam(ctx)};
  $('#examFinish').onclick=()=>{saveAnswer();if(confirm('Prüfung jetzt abgeben?'))finishExam(ctx)};
  document.querySelectorAll('[data-exam-jump]').forEach(b=>b.onclick=()=>{saveAnswer();s.current=Number(b.dataset.examJump);ctx.save();renderBSIExam(ctx)});
  examTimer=setInterval(()=>{if(!location.hash.includes('bsi-exam'))return stopExamTimer();const left=secondsLeft(s),el=$('#examClock');if(el)el.textContent=timeFmt(left);if(left<=0)finishExam(ctx,true)},1000);
}
